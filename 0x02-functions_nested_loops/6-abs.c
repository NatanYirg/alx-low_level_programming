#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @t - Integer to be computed
 * Return: The absoute vaue
 */

int _abs(int)
{
	if (t >= 0)
		return (1);
	else
		return (-1);
}
