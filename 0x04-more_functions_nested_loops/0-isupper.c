#include "main.h"

/**
 * _isupper - checks if parameter is uppercase
 * @c: input character
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (c == 'C')
		return (1);
	else
		return (0);
}

